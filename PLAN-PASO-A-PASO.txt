================================================================================
  PROYECTO FINAL - ASEGURAMIENTO DE CALIDAD DE SOFTWARE (PUCMM)
  Sistema de Gestión de Inventarios Empresarial
================================================================================

Equipo       : Jean + Emilio (2 integrantes)
Stack        : Spring Boot (Gradle) + React + PostgreSQL + Docker Compose
Inicio       : 8 de junio 2026
AVANCE 1     : 16 de junio 2026 (mínimo) — 18 de junio (máximo)
Entrega final: Principios de agosto 2026
Carga horaria: 20-25 horas/semana por persona
Enfoque      : Cumplir el PDF del avance ANTES de pulir el proyecto completo.

================================================================================
  ⚠️  LECTURA OBLIGATORIA — SITUACIÓN AL 10 DE JUNIO
================================================================================

El plan original de 8 semanas asumía backend/ con Maven y dejaba Keycloak,
Envers, Jenkins, Grafana y Playwright para julio. Eso NO sirve: el PDF del
avance exige TODO eso para el 16-18 de junio.

ESTRUCTURA REAL DEL REPO (no la del plan viejo):
  ProyectoQA/                    # Monorepo Gradle (NO hay carpeta backend/)
  ├── src/main/java/...          # Spring Boot en la raíz
  ├── build.gradle
  ├── infra/                     # Crear esta semana
  ├── frontend/                  # Crear esta semana
  └── .github/workflows/         # Crear esta semana

LO QUE YA ESTÁ HECHO (no repetir):
  [x] Proyecto Spring Boot Gradle + Java 21
  [x] Dependencias en build.gradle (Flyway, Envers, Security, Testcontainers…)
  [x] Migración V1: categories, products, stock_movements, revinfo, products_audit
  [x] Entidades: Product (@Audited), Category, StockMovement, MovementType
  [x] application.yml + application-local.yml (perfil local)
  [x] Repo público en GitHub (2 commits — faltan 13+)

LO QUE FALTA PARA EL AVANCE (todo lo demás del PDF):
  Ver checklist detallado en la sección siguiente.

REGLA PARA ESTA SEMANA:
  - NO reestructurar a backend/ con Maven. Seguir con Gradle en la raíz.
  - NO adelantar features del proyecto final que no estén en el PDF del avance.
  - Cada ítem del PDF = evidencia demostrable (screenshot, comando, curl, CI verde).
  - Meta commits: llegar a 15+ antes del 16. Meta PRs: 2 mergeados.

================================================================================
  CHECKLIST PDF — AVANCE 16-18 JUNIO
  (marcar [x] al demostrar con evidencia)
================================================================================

SEGURIDAD
  [ ] Keycloak funcionando (Docker, puerto 8081)
  [ ] Login OAuth2/JWT (frontend + API valida token)
  [ ] Al menos 2 usuarios creados (ej: admin, viewer)
  [ ] Al menos 2 permisos implementados (ej: product:view, product:manage)
  [ ] Endpoint protegido validando permisos (403 sin permiso, 200/201 con permiso)

BASE DE DATOS
  [x] Migraciones Flyway (V1 existe; agregar V2 seed si hace falta)
  [x] Mínimo 4 tablas relacionadas (categories, products, stock_movements, revinfo)
  [ ] Base de datos levantada mediante Docker

FUNCIONALIDAD
  [ ] CRUD Producto completo (Controller + Service + Repository + DTOs)
  [ ] Validaciones de negocio (SKU único, price >= 0, stock >= 0, etc.)

TESTING
  [ ] Mínimo 15 pruebas unitarias
  [ ] Cobertura mínima 60% (JaCoCo en capa service o global — verificar en CI)
  [ ] Evidencia de ejecución exitosa (screenshot o log de ./gradlew test)

GITHUB
  [x] Repositorio público
  [ ] README inicial completo (instalación, puertos, usuarios demo)
  [ ] Al menos 15 commits (ahora: 2)
  [ ] Al menos 2 Pull Requests mergeados

AUDITORÍA
  [x] Tablas Envers en migración
  [ ] Hibernate Envers funcionando (crear/editar producto → fila en products_audit)
  [ ] Evidencia de auditoría en base de datos (query o endpoint /audit)

API REST
  [ ] Swagger funcional (/swagger-ui.html)
  [ ] Todos los endpoints documentados (@Operation en cada uno)

INTEGRATION TESTING
  [ ] Testcontainers configurado
  [ ] PostgreSQL desde Testcontainers
  [ ] Mínimo 5 pruebas de integración

API TESTING
  [ ] Mínimo 10 escenarios (REST Assured o tests/@SpringBootTest)
  [ ] Validación de errores (400, 404)
  [ ] Validación de permisos (401/403)

DOCKER
  [ ] Aplicación dockerizada (Dockerfile + servicio en compose)
  [ ] Base de datos dockerizada
  [ ] Keycloak dockerizado

GITHUB ACTIONS
  [ ] Build automático
  [ ] Unit Tests automáticos
  [ ] Integration Tests automáticos

JENKINS
  [ ] Pipeline funcional (Jenkinsfile + al menos 1 ejecución con screenshot)

OBSERVABILIDAD
  [ ] Grafana configurado (Docker)
  [ ] Dashboard operativo (mínimo 1 dashboard con métricas del backend)

PLAYWRIGHT
  [ ] Login automatizado
  [ ] CRUD Producto automatizado

================================================================================
  REPARTO DE TRABAJO (sin cambios de roles)
================================================================================

Jean  = Backend, Docker/infra, Keycloak realm, GHA, Jenkinsfile backend, tests backend
Emilio = Frontend React, Playwright, Grafana dashboards, README, API test docs, Jenkins UI

Alternar quién abre PR: PR#1 Jean (10-11 jun), PR#2 Emilio (13-14 jun)

Comunicación mínima esta semana:
  - Daily async 15 min (ayer/hoy/bloqueo)
  - Pair session 2 h el 11 jun (Keycloak) y 2 h el 14 jun (CI + Docker full stack)

================================================================================
████████████████████████████████████████████████████████████████████████████████
  SPRINT AVANCE — 10 AL 16 DE JUNIO 2026
  Objetivo: checklist PDF 100% verde el día 16
  Presupuesto: ~30 h por persona en 6 días (pico de entrega)
████████████████████████████████████████████████████████████████████████████████

------------------------------------------------------------------------
DÍA 1 — MARTES 10 JUN (HOY) — Infra + esqueleto backend
------------------------------------------------------------------------

TAREA A1.1 [JEAN] — Docker Compose base
  - Crear infra/docker-compose.yml
  - Servicios: postgres:16-alpine (5432), keycloak:26 (8081:8080, start-dev)
  - Healthcheck postgres
  - Crear infra/.env.example
  - Probar: cd infra && docker compose up -d postgres keycloak
  - Commit: feat(infra): add postgres and keycloak services

TAREA A1.2 [JEAN] — Capas backend faltantes (sin auth aún)
  - CategoryRepository, ProductRepository
  - DTOs: ProductRequest, ProductResponse, PageResponse
  - ProductService: CRUD + SKU único + paginación + filtros (name, sku, categoryId, active)
  - ProductController: /api/v1/products (GET, GET/{id}, POST, PUT/{id}, DELETE/{id})
  - GlobalExceptionHandler (400, 404, 409 SKU duplicado)
  - SecurityConfig perfil local: permitAll (para desarrollar sin Keycloak hoy)
  - Probar con ./gradlew bootRun --args='--spring.profiles.active=local' + postgres Docker
  - Commit: feat(api): add product CRUD with validations

TAREA A1.3 [EMILIO] — Frontend mínimo + README
  - npm create vite@latest frontend -- --template react-ts
  - Routing: /login, /products (placeholder)
  - README: descripción, prerrequisitos, docker compose, ./gradlew bootRun, puertos
  - Commit: chore: init frontend and expand README

TAREA A1.4 [EMILIO] — GitHub hygiene
  - [x] CONTRIBUTING.md + Conventional Commits + hook .githooks/commit-msg
  - [x] Plantillas: .github/ISSUE_TEMPLATE/ + pull_request_template.md
  - [x] scripts/github-setup.sh (labels + milestone + 11 issues del sprint)
  - [ ] Crear rama develop, proteger main (PR + 1 review) — manual en GitHub
  - [ ] Ejecutar: ./scripts/github-setup.sh (requiere gh auth login)
  - [ ] Ajustar EMILIO_GH_USER=... si el username de Emilio es distinto
  - Commit: chore: add conventional commit hook and issue templates

CIERRE DÍA 1:
  - postgres + keycloak up
  - CRUD productos funciona en local sin auth
  - README con instrucciones básicas
  - Objetivo commits del día: 4-5 (total ~6-7)

------------------------------------------------------------------------
DÍA 2 — MIÉRCOLES 11 JUN — Swagger + Keycloak realm
------------------------------------------------------------------------

TAREA A2.1 [JEAN] — OpenAPI / Swagger
  - OpenApiConfig si hace falta
  - @Operation + @ApiResponse en ProductController
  - Verificar http://localhost:8080/swagger-ui.html
  - Commit: docs(api): document product endpoints in swagger

TAREA A2.2 [JEAN] — Keycloak realm (PAIR con Emilio 2 h)
  - infra/keycloak/realm-export.json
  - Realm: inventory
  - Client inventory-api (bearer-only) con roles: product:view, product:manage
  - Client inventory-frontend (public, PKCE)
  - Usuarios: admin/admin (todos los permisos), viewer/viewer (solo product:view)
  - Import: command start-dev --import-realm en compose
  - Commit: feat(security): add keycloak realm with users and permissions

TAREA A2.3 [JEAN] — Spring Security OAuth2 (perfil docker, no local)
  - application-docker.yml con issuer-uri Keycloak
  - JwtAuthenticationConverter: roles → PERM_product:view, etc.
  - @PreAuthorize en ProductController
  - POST sin product:manage → 403; GET con product:view → 200
  - Commit: feat(security): enforce JWT permissions on product endpoints

TAREA A2.4 [EMILIO] — Auth en React (PAIR tarde)
  - keycloak-js + AuthContext
  - Login redirect, logout, Bearer en axios/fetch
  - ProductsPage: tabla + crear/editar/eliminar (sin auth primero, conectar al API)
  - Commit: feat(frontend): keycloak login and products page

CIERRE DÍA 2:
  - Swagger documenta todos los endpoints de productos
  - viewer recibe 403 al crear; admin puede CRUD
  - Frontend hace login real
  - Objetivo commits acumulados: ~10

------------------------------------------------------------------------
DÍA 3 — JUEVES 12 JUN — Tests unitarios + Envers + Docker app
------------------------------------------------------------------------

TAREA A3.1 [JEAN] — Unit tests (meta: 15+)
  - ProductServiceTest: crear OK, SKU dup, update, delete, not found, filtros,
    validaciones negocio (mínimo 10 casos)
  - ProductControllerTest o mapper tests (5 casos más)
  - JaCoCo en build.gradle + reporte en build/reports/jacoco/
  - ./gradlew test → verde, coverage >= 60%
  - Commit: test: add product unit tests with jacoco

TAREA A3.2 [JEAN] — Envers evidencia
  - AuditService + AuditController GET /api/v1/audit/products/{id}
  - Crear producto → editar → consultar auditoría
  - Query manual: SELECT * FROM products_audit;
  - Commit: feat(audit): expose product revision history

TAREA A3.3 [JEAN] — Dockerfile backend + compose
  - Dockerfile multi-stage (gradle build → JRE 21)
  - Servicio api en compose (puerto 8080, depends_on postgres healthy)
  - Commit: feat(infra): dockerize spring boot api

TAREA A3.4 [EMILIO] — Dockerfile frontend + compose
  - frontend/Dockerfile (build + nginx)
  - Servicio frontend puerto 3000:80
  - Commit: feat(infra): dockerize react frontend

TAREA A3.5 [EMILIO] — UI permisos
  - Botón crear solo si product:manage
  - Página /unauthorized en 403
  - Commit: feat(frontend): hide actions by permission

CIERRE DÍA 3:
  - docker compose up --build levanta postgres + keycloak + api + frontend
  - 15+ unit tests, JaCoCo >= 60%
  - products_audit con filas tras editar producto
  - Objetivo commits acumulados: ~13

------------------------------------------------------------------------
DÍA 4 — VIERNES 13 JUN — Integration + API tests + PR#1
------------------------------------------------------------------------

TAREA A4.1 [JEAN] — Testcontainers (mínimo 5 integration tests)
  - AbstractIntegrationTest con @Testcontainers PostgreSQLContainer
  - ProductIntegrationTest: CRUD flow completo
  - SecurityIntegrationTest: 403/201 con JWT mock o @WithMockUser custom
  - AuditIntegrationTest: verificar tabla audit
  - FlywayIntegrationTest: migraciones aplicadas
  - Commit: test: add integration tests with testcontainers

TAREA A4.2 [JEAN] — API tests (mínimo 10 escenarios)
  - Puede ser misma suite con @SpringBootTest + MockMvc o RestAssured
  - Casos: CRUD OK, 404, 400 validación, SKU dup 409, sin token 401,
    viewer POST 403, admin POST 201, paginación, búsqueda, delete
  - Commit: test: add api scenarios for products and security

TAREA A4.3 [EMILIO] — GitHub Actions
  - .github/workflows/ci.yml
  - Jobs: build (./gradlew build -x test), unit tests, integration tests
  - Service container postgres para integration en CI
  - Commit: ci: add build unit and integration pipeline

TAREA A4.4 [EMILIO] — Playwright inicio
  - @playwright/test en frontend
  - e2e/login.spec.ts — login con admin
  - e2e/products.spec.ts — listar y crear producto
  - Commit: test(e2e): add playwright login and product crud

TAREA A4.5 [AMBOS] — PR #1
  - Jean: feature/avance-core → develop (backend + infra + tests)
  - Emilio review → merge
  - Objetivo commits acumulados: ~15+

CIERRE DÍA 4:
  - CI verde en GitHub
  - 5 integration + 10 API scenarios
  - Playwright login + CRUD pasa local
  - PR #1 mergeado

------------------------------------------------------------------------
DÍA 5 — SÁBADO 14 JUN — Jenkins + Grafana + seed data
------------------------------------------------------------------------

TAREA A5.1 [JEAN] — Jenkins pipeline
  - infra/jenkins/Jenkinsfile
  - Stages: Checkout → Build → Unit Tests → Integration Tests → Docker Build
  - Opción rápida: jenkins/jenkins:lts en compose con docker.sock
  - Screenshot pipeline verde
  - Commit: ci: add jenkins pipeline

TAREA A5.2 [JEAN] — Migración V2 seed (opcional pero útil para demo)
  - 3 categorías, 5 productos demo
  - Commit: chore(db): add seed data migration

TAREA A5.3 [EMILIO] — Grafana stack mínimo
  - Servicios: prometheus + grafana en compose
  - Scrape backend /actuator/prometheus
  - 1 dashboard JSON: JVM + HTTP requests + errores 4xx/5xx
  - http://localhost:3001 admin/admin
  - Commit: feat(obs): add prometheus and grafana with ops dashboard

TAREA A5.4 [EMILIO] — PR #2
  - feature/avance-frontend-ci → develop
  - Jean review → merge

CIERRE DÍA 5:
  - Jenkins ejecutado al menos 1 vez (screenshot guardado en docs/ o README)
  - Grafana muestra métricas del API
  - PR #2 mergeado
  - Objetivo commits acumulados: ~18+

------------------------------------------------------------------------
DÍA 6 — DOMINGO 15 JUN + LUNES 16 JUN — Buffer y evidencias
------------------------------------------------------------------------

TAREA A6.1 [AMBOS] — Verificación checklist PDF
  - Recorrer cada [ ] del checklist de arriba
  - Capturas: Swagger, Keycloak usuarios, products_audit, JaCoCo, CI, Jenkins,
    Grafana, Playwright report
  - Guardar en docs/avance-1/ (carpeta evidencias)

TAREA A6.2 [JEAN] — Fixes de integración
  - docker compose up --build desde cero en máquina limpia
  - CORS, issuer-uri, healthchecks

TAREA A6.3 [EMILIO] — README final avance
  - Tabla servicios/puertos
  - Usuarios demo y permisos
  - Comandos test: ./gradlew test, npm run test:e2e
  - Badge CI (opcional)

TAREA A6.4 [AMBOS] — Ensayo demo 10 min (interno)
  1. docker compose up
  2. Login admin → CRUD producto en UI
  3. Login viewer → 403 al crear
  4. Swagger + curl con JWT
  5. Query products_audit
  6. CI verde + Jenkins screenshot
  7. Grafana dashboard
  8. Playwright report

ENTREGABLE DÍA 16:
  -> Todo el checklist PDF marcado [x] con evidencia
  -> 15+ commits, 2 PRs mergeados
  -> Equipo puede explicar OAuth2, Envers, Testcontainers y CI

================================================================================
  MAPA RÁPIDO: PDF → DÓNDE SE CUMPLE
================================================================================

  PDF                          Implementación / evidencia
  ─────────────────────────────────────────────────────────────────────────
  Keycloak                     infra/docker-compose.yml + realm-export.json
  OAuth2/JWT                   SecurityConfig + keycloak-js frontend
  2 usuarios                   admin, viewer en realm-export.json
  2 permisos                   product:view, product:manage en JWT roles
  Endpoint protegido           ProductController @PreAuthorize
  Flyway                       src/main/resources/db/migration/
  4 tablas                     V1__init_schema.sql
  DB Docker                    servicio postgres en compose
  CRUD Producto                ProductController + ProductService
  Validaciones                 Service + @Valid en DTOs
  15 unit tests                src/test/.../ProductServiceTest.java
  60% coverage                 build.gradle jacoco + report HTML
  README                       README.md
  15 commits                   git log --oneline | wc -l
  2 PRs                        GitHub PRs mergeados a develop
  Envers                       @Audited Product + products_audit
  Swagger                      springdoc + @Operation
  Testcontainers               AbstractIntegrationTest
  5 integration tests          *IntegrationTest.java
  10 API scenarios             ProductApiTest o MockMvc suite
  Docker app/db/keycloak       compose + Dockerfiles
  GHA                          .github/workflows/ci.yml
  Jenkins                      infra/jenkins/Jenkinsfile
  Grafana                      infra/grafana/ + dashboard JSON
  Playwright                   frontend/e2e/*.spec.ts

================================================================================
  POST-AVANCE — SEMANAS 17 JUN EN ADELANTE (proyecto completo PDF final)
================================================================================

Una vez aprobado el avance del 16-18 jun, retomar el ritmo de 20-25 h/semana
para el proyecto FINAL (agosto). Prioridades post-avance:

  Semana 17-21 jun  Stock completo, más permisos (stock:*, audit:*, report:*)
  Semana 22-28 jun  Dashboard negocio, alertas stock mínimo, más E2E
  Semana 29 jun-5 jul Observabilidad completa (Tempo, Loki, Alloy, 4 dashboards)
  Semana 6-12 jul   Testing masivo: k6, ZAP, exploratory, SonarCloud
  Semana 13-19 jul  Staging, deploy post-tests, pipeline completo GHA+Jenkins
  Semana 20-26 jul  Documentación final, trazabilidad requisitos, presentación
  Buffer 27 jul-4 ago  Solo fixes críticos

NOTA: Las semanas 1-8 del plan original siguen siendo referencia de contenido
(funcionalidades, permisos completos, observabilidad avanzada), pero el
CRONOGRAMA quedó adelantado: lo que el plan ponía en semanas 3-7 para el avance
del 16 jun ya está concentrado en el sprint de arriba.

================================================================================
  COMANDOS DE REFERENCIA (Gradle — NO usar mvnw)
================================================================================

# Infra
cd infra && docker compose up -d --build

# Backend local (sin auth estricta)
./gradlew bootRun --args='--spring.profiles.active=local'

# Backend con Docker profile (con Keycloak)
./gradlew bootRun --args='--spring.profiles.active=docker'

# Tests
./gradlew test
./gradlew test jacocoTestReport
open build/reports/jacoco/test/html/index.html

# Frontend
cd frontend && npm install && npm run dev
cd frontend && npm run test:e2e

# Ver commits
git shortlog -sn
git log --oneline | wc -l

# Crear feature
git checkout develop && git pull
git checkout -b feature/nombre-tarea

================================================================================
  RIESGOS ESTA SEMANA (16 jun)
================================================================================

RIESGO                    MITIGACIÓN
──────────────────────────────────────────────────────────────────────────
Keycloak bloquea 1+ día   Pair session día 2; realm-export.json versionado
Poco tiempo               Jean solo backend día 1-3; Emilio paralelo frontend
Solo 2 commits            Commits pequeños diarios (convencional commits)
CI falla integration      postgres service en GHA; Testcontainers en local+CI
Jenkins sin máquina       Jenkins en Docker con socket; 1 screenshot basta
Grafana overkill          1 dashboard Prometheus basta para el avance
Playwright flaky          Usuarios seed fijos; retries: 2 en config

================================================================================
  REGLAS DE TRABAJO (sin cambios)
================================================================================

1. Conventional Commits: feat:, fix:, test:, docs:, chore:, ci:
2. Ramas: main (protegida) ← develop ← feature/*
3. Cada tarea importante = issue + PR
4. Ningún PR sin review del compañero
5. NO saltar evidencias: si no se puede demostrar, no está hecho

================================================================================
  FIN — PRIORIDAD ABSOLUTA: CHECKLIST PDF VERDE EL 16 DE JUNIO
================================================================================
